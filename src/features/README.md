# Feature modules

Add domain features here as the product grows, for example:

```text
features/
  auth/
  billing/
  dashboard/
```

Keep each feature self-contained (`components`, `hooks`, `lib`, `types`) and import into `app/` routes.
